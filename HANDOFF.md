# 环形菜单与次级入口

版本：2026-09-14。独立复制自“companion-ui-fox_菜单直达与异常精简_2026-09-14”，不覆盖旧版。

## 页面组织

165 个独立画面，12 个模块，全部在 gallery.html 内连续上下滚动。
保留原页码，不新增分页，不将同一菜单拆成“更多”页面。

- 每个模块集中展示主流程、分支、相关状态和异常恢复。
- 首次连接超时 P37 紧接主设备连接流程；配网错误和服务重试就近提供共用预览。
- 录音顺序为录制与保存、暂停/提醒/低亮状态、未保存退出、保存异常、备份、回放、删除、录音后的提醒处理。P173 备份权限与 P171/P72 备份结果合并展示，设置保留就近共用预览。
- 照片生成 P51/P52/P53、角色启用 P154/P155 及启用异常 P156 归入我的伙伴模块；好友共同创作旁保留启用与失败共用预览。
- 好友权限、关系变化、生成失败放在各自操作附近；碰一碰与组队旁补齐邀请结束预览，添加宠物旁补齐伙伴选择与状态预览。
- 计步异常紧接计步流程；等级成长归入虚拟伙伴模块。
- 设置中的语言与角色选择、提醒详情中的播报，以及首次配网、对话、陪伴、录音、好友、换网和回忆中的共用状态，就近提供共用画面预览。不是新页面，不复制状态机。
- 共用画面保留原页码，DOM ID 使用所在流程后缀；原始页面索引与目录前后导航仍各有唯一归属。
- P186 在伙伴模块提供就近共用预览；P27 已从规范页和全部共用预览移除，不计为页面。
- 页面图板保持相同比例与尺寸；共用画面在同一小节网格中接续展示，不再另起一组网格，页码旁标注“共用”且不编主流程步骤号。模块之间保留分界。

共用预览仍打开对应页面的审阅样例。验证真实来源、重试与返回路径，应从该模块入口操作；直接打开异常样例不等同于经历了真实失败过程。

## 菜单与计步

P10 待机 → P74 环形菜单 → P187 日常 → P103 今日步数。
P187 日常 → P77 提醒中心；提醒与步数返回 P187，再返回 P74。待机消息快捷入口仍直接进入 P77，返回原来的待机页。

菜单保留七入口环形布局和原图标尺寸，智能对话居中。“提醒”改为“日常”，采用日历图标，内部为提醒与今日步数两行列表。我的伙伴入口保持不变，不与步数关联。
待机页移除脚印，不新增主菜单图标。日常模块集中展示入口、提醒、计步及相关异常；entry=steps 旧链接转向 P187 日常。
计步失败与重试不增加返回层级，返回直接到日常菜单。
计步数值仍为原型样例，不代表真实设备数据。

## 等级成长

菜单顶部仅显示“我的伙伴”，不附加等级文字；点击进入 P93 伙伴页后查看当前等级并进行照顾。P18 显示“升级啦”和新等级；关闭升级反馈后 P93 显示新值，返回 P74 仍不显示等级。审阅样例默认等级 1，升级示例为等级 2。

- LEVEL_UPDATED 接收当前角色的 avatarId 与正整数 level。
- 只有等级高于当前记录时才触发；重复、下降、无效等级及其他角色回执被忽略。
- 待机或伙伴总览可展示升级；录音、对话、保存等任务期间不打断，回到可展示位置后呈现。
- 完成返回触发时的待机或伙伴总览；角色等级分别保存在角色状态中。
- 本版没有接入真实成长服务，没有定义经验值计算、升级门槛或等级解锁能力。这些规则需产品另行确认，照顾动作不会自行增加等级。

## 切换伙伴

P74 我的伙伴 → P93 上方伙伴展示区左右滑动 → 即时切换，留在 P93。
P93 下方照顾列表 → 管理伙伴 → P186 本地伙伴与待启用角色 → 确认或取消 → P93。

- P93 移除顶部“切换伙伴”按钮。在伙伴展示区左滑切换下一个、右滑切换上一个，首尾不循环；左右箭头是点按替代，键盘左右方向键在伙伴区聚焦时切换。显示当前位置，只有一个伙伴时两侧箭头禁用。
- 横向切换只作用于上方伙伴展示区；照顾列表保持上下滚动，其他区域右滑和返回按钮仍按原来源返回。短于 40 画板像素（480 画板基准，按显示缩放换算）、纵向或斜向为主的滑动不切换；取消、失焦、页面离开或旧角色发起的迟到手势不切换，不触发松手位置的照顾按钮。
- 复用 P186 作为“管理伙伴”，不新增页面；保留查看待启用照片任务及明确确认选择的入口。伙伴包含 Lumi / Pico / Momo 三个预置伙伴，以及已成功启用或添加到本机的伙伴。
- `availableCompanions` 仅登记预置伙伴、有效 `AVATAR_ACTIVATED` 启用的生成伙伴，以及有效 `PEER_IMPORT_SUCCESS` 添加的好友宠物。仅生成完成、尚在云端或待启用的结果不进入切换列表，不绕过资源校验和启用流程。
- P93 滑动或点按箭头直接切换；P186 管理页内点选只更新待确认选择，确认后显示该伙伴，取消保留原伙伴。等级、照顾数值和冷却按 `avatarId` 独立保留，切换不重置、不复制，也不自行增加等级，不改变食物库存和返回来源。
- 首次设置仍为 P186 → P07 欢迎；从设置更换后沿用返回 P10 待机，取消回 P180。伙伴页来源确认和取消均回 P93，并保留伙伴页原有返回来源。

## 好友导航（2026-09-15）

P74 好友 → P185 好友菜单。菜单固定为“碰一碰交友”“我的好友”“好友消息”三个入口，不将 Milo 等具体好友与功能入口并列。

- “我的好友”进入新增 P191，集中展示已确认的好友。列表支持多个好友滚动，每行显示好友名称、宠物缩略图和宠物名称；未确认的关系不进入列表。
- P191 → P56 好友详情 → 返回 P191，保留选中的好友和列表滚动位置；再次返回 P185，再回到原菜单来源。查看权限或关系异常后也返回原好友列表，不跳过该层级。
- 无好友时显示“还没有好友”和“碰一碰交友”入口。从空列表开始交友，取消返回 P191；双方确认成功后进入 P56，返回即可在 P191 看到新好友。从 P185 开始的交友流程仍返回 P185。
- P165 统一命名“好友消息”，承接邀请、组队和共同创作等原有事件，不新增聊天功能。请求有新的通知版本时，P185 对应入口显示未读圆点；进入好友消息页后标为已读。在消息页收到更新视为已读，重复旧回执不重新点亮。
- P185 与 P191 在好友模块连续展示，好友详情、消息及相关异常仍集中在同一模块。P191 有独立目录页和深链接，不复制 P185 菜单，也不改动社交授权条件。
- Milo 仍是原型演示数据。真实好友来自两台设备碰一碰后的双方确认，不因打开菜单、列表或详情自动建立关系。

## 碰一碰与添加伙伴

P74 好友 → P185 碰一碰交友 → P54 等待接触 → P55 本机确认 → P176 等待对方确认 → P56 好友详情。
P56 添加宠物 → P188 确认 → P189 添加中 → P56 已添加；失败进入 P190，重试或取消仍在好友模块内闭环。

“好友”表示已确认的设备社交关系；“添加宠物”表示将该好友的宠物加入本机“我的伙伴”，不是再次加好友。按钮改为“添加宠物”，确认文案明确“将对方宠物添加到本机？”，保持原有确认、取消和资源回执校验。

- 两台设备接触只提供发现结果，不代替本机和对方确认；社交未授权时进入原权限说明，不能靠圆屏按钮制造授权。
- 好友请求保存设备、宠物和发现会话快照。重复发现、自身设备、无效标识，以及取消、过期后的回执均不能创建好友。
- 添加的是对方已有宠物，不等于一起生成新角色。添加成功仅加入列表，不自动替换当前伙伴；在好友详情点击“我的伙伴”，选择并确认后才切换。
- 同一对方设备与宠物组合只添加一次。失败、超时、取消、权限撤销及过期回执不产生半成品伙伴，不改变当前伙伴及其独立等级。
- 后台交友成功仅更新好友列表和邀请通知，不切换当前查看的好友，也不干扰正在发送的问候或添加的伙伴。问候回执匹配发送时的收件人，不能被之后选中的好友替代。
- 多个好友分别添加后，从某位好友打开“我的伙伴”会预选该好友的宠物，不使用最近一次添加结果。P56 的“更多互动”进入 P87，再选择组队或共同创作，返回保留当前好友。
- 原型侧栏明确模拟“设备碰一碰”和“对方确认”；没有接入真实近场发现、设备身份认证、资源传输或账号服务。`source: sample` 的 Milo 为原有演示好友，不是已联网查询的真实关系。

## 喂养与购买库存

P74 我的伙伴 → P93 喂养 → 无库存 P157 提示 → 在 App 购买 → App 同步库存 → P94 喂养确认 → 喂养反馈回 P93。

- 设备默认没有食物库存；设备不提供购买、赠送或无限喂养入口。购买发生在 App，App 只同步本设备已购买的累计数量。
- 每次有效喂养消耗 1 份库存。设备先校验当前伙伴、饱腹值、冷却、休息状态、动作版本和库存，再一次性提交；重复点击、重复回执和过期动作只执行一次。
- 库存更新只接受本设备、更高版本、累计已购数量不下降的回执，并保留已消耗数量。主动同步时，同版本且累计数量完全相同的回执可结束当前请求；它必须匹配有效 requestId，不能增加库存或重置已消耗数量。旧回执、伪造设备标识、降级数量和无效数据不会补回库存。
- 库存为 0 时进入 P157，提示在 App 购买并提供“同步购买内容”；同步失败留在 P157，可重试。有效同步后仅在库存大于 0 且当前伙伴满足喂养条件时回到 P94；库存仍为 0 则留在 P157，不会自动喂养。
- 同步请求 15 秒无回执即显示失败，重复点击不重复建请求；主动请求的回执匹配 requestId，重试后旧请求失效。已吃饱、冷却或休息时不会因为库存同步而进入可喂养状态。
- 喂养成功后库存和伙伴饱腹度同时更新；库存耗尽、已吃饱、冷却中、休息中或安全中断不扣库存。同步断网不会增加或清空库存；已确认的本机库存可离线使用。库存共享给本设备的伙伴，伙伴状态仍独立保存。
- 当前原型只模拟 App 已购买库存同步，不接入支付、订单或真实商店；量产需补充订单校验、退款/撤销、账号与多设备归属规则。

## 交付前补齐的交互

### 照片角色

App 发起 → P51 等待照片 → P52 生成 → P53 结果 → P154 确认 → P155 启用 → P93。
“稍后”回实际来源，不回生成中，也不丢任务。P74 → P93 → 管理伙伴 P186 中可打开未启用照片任务；P77 消息列表也可查看。只有明确启用成功后，结果才加入可切换伙伴。

- 照片任务独立于交友请求保存。生成失败或等待 120 秒无结果复用 P102 的照片状态；“查看状态”查询同一任务，使用新尝试编号，不新建付费生成。取消查看不取消后台任务。
- P53/P154 显示所选结果名称；启用确认和失败重试锁定同一结果，其他后台结果不会替换目标。成功、失败及处理中页面均保留可恢复入口。
- `REVIVE_REQUESTED`：`deviceId`、唯一 `requestId`。设备登记后返回本次 `attemptId`，App 提交照片通过 `REVIVE_START` 携带这两个编号。
- `REVIVE_SUCCESS`：`requestId`、`attemptId`、唯一 `resultId`、`characterName`（1–40 字符）；失败使用 `REVIVE_FAILED`。缺字段、旧尝试、重复结果及超时回执不启用任何默认角色。
- 照片模块就近显示 P102 失败与 P186 稍后查看的共用样例；链接携带 `entry=photo`，不会误入好友生成失败。

### 回忆同步

- 每次确认或恢复同步生成新 `sessionId`，等待、传输、校验回执都必须匹配本次会话。取消、完成、重启或安全中断使旧编号失效。
- `SOUL_READY`、`SOUL_TRANSFERRED`、`SOUL_VERIFIED`、`SOUL_INTERRUPTED`、`SOUL_VERIFY_FAILED` 均携带 `sessionId`，只在对应阶段处理。不再接受无编号回执。
- 等待设备最多 120 秒；传输与校验合计最多 120 秒。超时进入 P39，可恢复或返回；恢复重新生成会话编号，旧回执不能推进新任务。

### 提醒与中断

- `NOTE_REMOTE_CREATE` / `NOTE_REMOTE_UPDATE`：`noteId`、正整数 `revision`、`note`。首次创建需标题 `noteTitle`（1–200 字符）及到期时间 `noteDueAt`；状态为 scheduled / due / snoozed / completed，snoozed 需有效 `snoozeAt`。时间目前使用原型时钟秒值，设备接入须统一映射到持久化时间基准。
- 更新只接受更高版本；`NOTE_REMOTE_DELETE` 携带编号和版本，未知编号也保留删除版本，迟到旧创建不能恢复已删除提醒。
- 本机完成、稍后操作立即反映在设备上，同时保留待同步记录。离线排队，联网重试；失败或 15 秒无回执保留记录，可在 P77 的“同步提醒”重试。
- `NOTE_SYNC_RESULT` 携带 `operationId`、当前 `attemptId`。只有 `success: true` 且返回更高 `revision` 才移除记录；失败、缺字段和旧尝试不得当作成功。同一提醒的多个操作按顺序发送。
- 冲突回执携带 `conflict: true`、更高版本与服务端 `note`（删除则 `deleted: true`）；采用服务端状态，详情展示 P159 更新反馈。
- 设置、配网、伙伴操作、同步和其他任务期间，提醒先排队，不跳离当前页或清空输入。回到菜单、待机或陪伴空闲页后再提示，处理后回该真实来源。录音仍沿用原有录音提醒排队流程，安静模式仍抑制提示。

以上均为原型中的流程与回执协议模拟，不代表已接入 App、云服务、真实照片生成或持久化。升级门槛、订单与退款、多设备库存、陪伴授权与算法，以及其余四种语言的翻译仍需单独确认，不由本次补设。

## 本次闭环修复

- P154 进入确认时锁定角色名称和标识，P156 启用失败后的重试保持同一角色。后台其他生成结果只更新各自请求；明确从好友消息打开另一结果后才更换启用目标，迟到回执不能替换确认中的角色。
- 修复后台交友回执改变当前好友、导致问候一直停留在 P137 的问题；原发送请求仍按自己的收件人结束，新好友可从列表或邀请动态明确打开。
- 修复库存无变化时被误报为同步超时的问题：App 有效返回相同版本、相同累计数量后结束加载；库存仍为零时留在 P157 的购买提示，不制造食物，也不自动喂养。
- 设备上的实际操作启动任务后，流程模式自动运行计时；目录预览、历史快照和用户明确暂停保持静态。对话、网络扫描、提醒播报、录音回放、伙伴休息及社交发送不再等待外部手动打开原型时钟。
- 修复通用设置与隐私的重复返回栈、回忆同步失败重试后返回旧失败页、好友详情和问候返回跳过好友菜单、录音结果与空列表新录音的返回来源。
- 问候发送期间权限或关系失效时结束发送并展示可恢复结果，不停留在没有任务的发送中。
- 交友明确区分对方拒绝、邀请超时和本机取消；收到已有对方同意的创作邀请后，本机接受即开始生成，不再要求对方确认第二次。
- 修复临界低电量关机动作；过热和系统恢复页不显示被安全保护禁止的无效返回按钮，必须满足冷却或恢复条件才能退出。
- 新增页面与错误状态放在所属模块；保留旧版精简、独立滑杆、陪伴开关和七入口环形菜单。

## 陪伴开关

P74 陪伴 → P82 关闭状态 → 打开开关 → P58 开启状态。取消原确认页和 30 分钟倒计时；关闭立即回到开关关闭状态，旧 P160 链接转向 P82。

- careEnabled 表示用户开关意愿，careActive 表示此刻是否在监听，两者分别管理。
- 开启后在待机、主菜单和陪伴页持续监听；返回菜单不会关闭。菜单显示开启标记，待机状态栏显示声音图标。
- 录音、对话、安抚播放、操作其他功能或息屏时暂停监听，回到可监听页面时恢复。硬件静音、重启、关机与安全中断直接关闭开关，不自动重新开启。
- 有效声音线索触发 P59 中性安抚，6 秒后返回原页面；可主动选择呼吸引导。P60–P66 不显示“完成”按钮，只保留返回；播放 12 秒后自动返回触发前页面并继续陪伴，返回可提前结束本次反馈。反馈期间不收音，相近触发至少间隔 30 秒。
- 监听和反馈分别带会话标识；暂停、关闭或重新开启使旧回执失效。未开启时不接受情绪或声音回执，不因返回、解除静音或迟到回执启动监听。
- 情境安抚只使用用户主动表达或明确选择，不根据声音断言“生气、被拒绝、发生冲突”，不诊断情绪或心理疾病，不代替现实照护。
- 本版仅模拟开关、声音事件与反馈，不访问麦克风、不生成真实情绪判断、不保存或上传环境录音。量产仍需确认识别算法、授权机制、阈值与耗电。

## 延续的精简

P180 的入口与 P75 页内标题统一为“通用设置”。P75 移除重复的无线网络入口，配网仍从 P180 进入。
P75 分别提供音量与亮度入口：P31 音量、P32 亮度各显示图标、单条滑杆与当前数值；调节即时生效并保留，不需要完成按钮。返回 P75 可看到最新值，再返回真实进入来源。旧 P117 合并页从目录移除，旧链接及设备调节按键转向 P75。
音量范围 0% 至 100%，亮度范围 10% 至 100%；两项互不影响，原型只更新演示状态。滑杆支持原生点按、拖动与键盘输入，拖动期间保持当前控件，避免模拟计时重绘中断操作。

P75 通用设置移除麦克风入口，P27 硬件静音页面及全部共用预览彻底移除，旧 P27 链接转向 P75。硬件静音仍优先中断采集；解除静音不会自动重开任务，也不等同于输出音量为零。

- 录音入口 P70 和陪伴开关 P82 在硬件静音时留在当前页面内联提示，不开始收音。
- 智能对话入口 P161 被阻断，或 P14 聆听时收到硬件静音，都结束本次对话采集并返回实际对话来源 `aiOrigin`，在来源页内联提示；独立样例默认回 P10。
- 既有录音片段仍按录音模块的暂停、保存和退出流程处理；移除页面不删除草稿，也不新增静音确认步骤。

删除 P113 无线网络连接失败和 P166 天气不可用画面，包括全部共用预览。旧链接分别转向网络列表与待机。
无线网络超时返回本次 P05/P109 列表，显示“连接超时，请重新选择网络”，清除失败连接标识和密码；再次点击网络后重新连接，旧回执无效。
P78 云端不可用只保留“再试一次”，没有继续、跳过或可见返回按钮。重试进入 P22 检测：服务仍不可用则回 P78，恢复后按首次设置进度继续或回到日常待机。不将服务故障误判为密码错误。

P119/P120 黑屏不作为审阅画面，P38 合并到 P37 超时并保留重试；预置角色下载页继续隐藏。
陪伴不显示“麦克风开启/已停止收音”或倒计时；P82 移除重复的关闭状态文字，保留开启前的监听说明；P59 不显示麦克风图标。实际监听、暂停及硬件阻断状态由原页面状态反馈表达。

## 底座与充电状态

P23 入座充电提示 → 1.5 秒后自动进入 P24 充电中 → 电池满电回执后显示 P26 已充满。拔出底座恢复 P10 普通待机。充电、满电、内容同步及对应恢复状态在同一个系统模块相邻展示。

- P23 只作短暂确认，无继续按钮；同次稳定入座不重复播放。P23 / P24 / P26 使用独立充电状态页，不复用待机的宠物、时钟和底部快捷栏；页面突出电池环、充电状态、电量和底座提示。上滑或点击底部箭头可打开应用菜单，充电状态页本身不承担待机内容。
- 充满仅表示本次电池状态为 full 且电量为 100%，不表示内容同步完成。充电暂停显示“充电已暂停”；不会自动假设温控解除后已经恢复充电。
- 录音、对话、设置、密码输入、伙伴操作或系统任务中入座，只更新充电状态，不跳转、不清空输入、不停止录音或对话。原操作结束后回待机时，按当前状态进入 P10 / P24 / P26，旧返回目标不导致错误的满电画面。
- 保留既有自动息屏时长和唤醒行为。选择黑屏时仍正常息屏；选择低功耗显示时，充电期间使用静态电量与充电状态，不显示待机时钟，拔出后才恢复普通时钟。电量回执不重置无操作计时，不唤醒息屏设备，也不取消手动息屏。充电时依旧可以进入菜单及使用功能。
- 稳定接触失败显示 P45；在忙碌页面中发生则延后到空闲待机，不抢占操作。P48 过热和 P34 临界低电量仍受安全状态保护，仅插入底座或收到满电回执不能自动解除保护。

设备侧事件约定：

- `DOCK_CONNECTED` 是设备完成稳定供电检测后的本机事件，不是点击界面表示“已经充电”。状态机生成新的 `dock.sessionId`；重复稳定接触不新建会话。断开或接触故障后重新入座创建新会话。
- `CHARGE_UPDATED` 携带当前 `sessionId`、严格递增的正整数 `revision`、0–100 的整数 `battery` 和 `status: charging | full | paused`。full 必须为 100%；无效数据、旧版本和旧会话不改变状态。
- `DOCK_REMOVED` / `DOCK_CONTACT_ERROR` 携带当前 `sessionId`，立即停止显示有效充电并使旧提示和同步结果失效。重连后迟到的旧断开事件不影响新会话。
- `DOCK_FEEDBACK_DONE` 为本机 1.5 秒计时事件，必须匹配当前会话且仍在 P23；提前、迟到或重复事件不能强制返回待机。
- 内容同步 P25 是独立流程。`DOCK_SYNC_START` 表示已有授权的内容同步开始，入座本身不授予访问或同步权限；生产适配层必须先完成授权及内容差异校验。P46 / P47 恢复或重试创建新 `attemptId`。`DOCK_SYNC_DONE` / `DOCK_SYNC_FAILED` 同时匹配底座会话与同步尝试；120 秒无结果进入失败恢复。移除底座暂停同步，保留原内容；重新放稳后由用户恢复，不自动覆盖。
- P25 稍后可返回待机，后台完成不跳离其他功能；同步成功只结束同步，不改写电量。温控、关机和重启使未完成同步暂停，旧结果不再生效。

原型验证入口：交互页侧栏的“底座事件模拟”提供放入、拔出、充满和接触异常；这些不是设备屏内按钮。流程模式直接打开 P23 或实际触发入座后自动计时；目录预览、历史回看和手动暂停仍保持静态。此版不连接真实底座、电池管理或同步服务，电量与内容进度仍为模拟数据，不替代硬件验收。

## 验证与文件

- verify-complete.cjs：Node 状态机、DOM、资源与 CSS 规则回归。
- verify-flow-closure.cjs：逐页按钮、输入、自动任务、七个实际菜单入口与异常往返审计。
- verification.json：实际运行结果与失败详情。
- flow-closure-audit.json / flow-closure-audit.csv：逐页检查记录；区分设备按钮、静态样例与依赖 App / 硬件回执的路径，不把目录可打开当作实际入口已验证。
- flow-order.json：模块、阶段、规范页码及就近共用预览。
- page-inventory.csv：唯一页面清单，不重复统计共用预览。

当前环境不允许浏览器访问本地原型，未进行浏览器截图及实际像素布局验收。DOM/CSS 检查不等同于视觉验收，也不代表真实硬件、云端服务或量产流程认证。
