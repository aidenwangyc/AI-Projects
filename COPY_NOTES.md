# 简中文案优化说明

本文保留“情感文案优化”的文案原则。当前版本在菜单配网精简版上另存，进一步统一录音与好友的归类、名称和返回路径，并从日常菜单移除继续设置；当前页面与流程以 `HANDOFF.md` 为准。

文案对照以“主设备配网_交互修订_2026-09-12”为旧基线，不覆盖任何旧版。面向 10-12 岁儿童，保持 Fox 原有视觉、全部页面纵向浏览和圆屏列表上下滚动。

## 文案方向

- 设备陪伴孩子时用“陪伴”；孩子照顾虚拟伙伴时用“喂食、清洁、休息、探索”，两种关系不混用。
- 问候、邀请和完成反馈用自然的短句。不增加长段解释，不堆叠语气词，不用依赖暗示或诊断式判断。
- 按钮优先说明动作：保存、删除、结束、查看。陪伴可以拒绝，先不用在左、开始陪伴在右。
- 到期列表只保留事项；混合消息中保留待处理、未到时间、稍后、已完成，不能因精简文案丢失状态。
- 隐私、风险及错误原因不拟人化：麦克风开启、声音发送到在线服务、删除后无法恢复、录音保留期限等信息保留。

## 主要变化

| 场景 | 优化后的画面文案 |
| --- | --- |
| P74 菜单 | 恢复首页和陪伴的原始入口含义；P108、P116 合并，照顾伙伴与今日步数在同页下方 |
| P128 / P174 网络 | 密码错误按钮“重新输入”；名称详情“网络名称 / 当前所选网络”，完整显示原始名称 |
| P167 到期提醒 | “待办提醒”及事项名称，不重复显示“现在到时间”；空状态“暂无待办提醒” |
| P82 陪伴确认 | “要我陪你一会儿吗？” / “麦克风将开启 30 分钟” / “先不用、开始陪伴” |
| P58 陪伴中 | “我在陪着你”，保留剩余时间、真实收音状态和结束按钮 |
| P59 休息邀请 | “一起歇一会儿吗？” / “先不用、一起试试” |
| P60-P66 放松与支持 | 如“和我一起慢慢呼吸”“歇一会儿也没关系”“找信任的大人聊聊”；结束动作明确写“结束” |
| P14-P16 对话 | “我在听 / 让我想想 / 说给你听”；收音与暂停收音按实际状态显示 |
| P43 回答超时 | “这次没能回答”，不再表示仍在思考；重试与结束保留 |
| P17 小分享 | “想和你分享”，避免“稍等一下”被误解为加载等待 |
| P70 / P123 / P73 录音 | “想记下什么呢？” / “录音存好啦” / “录音还没存好” |
| P168 录音过期 | “录音已过期” / “超过 24 小时，已自动删除” |
| 宠物与好友 | “干净啦”“精神满满”“打个招呼”“你们是队友啦” |
| 回忆 | 使用“回忆”作为儿童端名称；保留“原有回忆会保留”等数据安全提示 |

`copy-before-after.csv` 提供当前 170 页的旧版/新版可见文字与按钮名称；`copy-review.json` 汇总实际变化的页码。对照基于目录样例，不包含所有用户自定义内容及运行时组合。P07 的自我介绍已支持当前角色名字。

## 实现与保留项

页面文案修订在 `fox-data.js`，简中词条在 `locale-cn.js`；动态提醒、麦克风提示和宠物状态在 `screen-renderer.js` 与 `fox-renderer.js`。开发时应使用运行时合并数据，不要只读取历史基线 `project-data.json`。

陪伴内容区保持固定范围，麦克风状态独立一行；主要新增简中提示控制为短句，按钮不因文字变化缩小热区。英文录音提示允许两行并预留与主按钮的间距。这里的静态字符和几何检查不代替字体实测。

此前情感文案修订不改变协议；本轮仅按新标注调整二维码有效期逻辑、菜单和网络页面路由，详见交付说明。权限、录音期限与养成冷却不变。“同步回忆”仍指既有单向复制和校验流程，不新增双向合并。录音“存好啦”仍配有仅本设备/上传中/云端备份状态，不能据此推定云端已保存。

保留主设备确认、圆屏选网和输入密码、AI 直接对话、无触屏教学、左上角返回、取消左确认右、单按钮居中。繁中、德语、法语、西班牙语仍仅补齐语言选择项，未新增整套翻译。

## 验证边界

运行 `node verify-complete.cjs` 查看 `verification.json`。覆盖旧版文件保护、页面和分支完整性、中英 DOM 渲染、文案前后对照、提醒处理、陪伴同意/拒绝/结束/超时/静音/录音冲突、隐私提示与动态保留期限。

本轮未完成浏览器截图、真实字体布局或物理圆屏触控验收，原因是浏览器安全策略限制。不能以自动检查通过声称已满足儿童可用性或上市条件；仍需在目标硬件上检查圆形裁切、字体、热区，并观察目标年龄儿童完成真实任务。
